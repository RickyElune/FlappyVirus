import {heightRatio, widthRatio} from './styleSheet';

const Constants = {
  TOP_PIPE_WIDTH: widthRatio * 120,
  BOTTOM_PIPE_WIDTH: widthRatio * 120,
  TOP_PIPE_HEIGHT: heightRatio * 120,
  BOTTOM_PIPE_HEIGHT: heightRatio * 120
};
export default Constants;
